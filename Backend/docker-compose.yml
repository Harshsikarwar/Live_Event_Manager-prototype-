version: "3.9"

services:
  backend:
    build: .
    container_name: django_backend
    command: python live_event_manager/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"